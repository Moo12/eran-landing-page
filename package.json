{
  "name": "eran-landing-page",
  "version": "1.0.0",
  "description": "ערן ישראלי — website and admin panel",
  "scripts": {
    "setup": "cd api && npm install && cd ../admin && npm install && cd ../api && node seed.js",
    "dev:api": "cd api && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "build:admin": "cd admin && npm run build",
    "start": "cd api && node index.js",
    "seed": "cd api && node seed.js"
  }
}
